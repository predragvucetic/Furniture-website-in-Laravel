$(document).ready(function() {

    /*fetchProductsData();

    function fetchProductsData(keyword = "") {
        $.ajax({
            url: "{{ route('/products') }}",
            method: "GET",
            data: {
                keyword: keyword
            },
            dataType: "json",
            success: function (data) {
                $(".block2").html(data.search);
            }
        })
    }

    $(document).on("keyup", "#search-product", function () {
        let keyword = $(this).val();
        fetchProductsData(keyword);
    })*/


})
